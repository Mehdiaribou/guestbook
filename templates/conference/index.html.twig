{% extends 'base.html.twig' %}

{% block title %}Conference Guestbook ✅
{% endblock %}

{% block body %}


    <h2>Give your feedback!</h2>
    {% for conference in conferences %}
        <h4>{{ conference.toString }} <span>
            <a href="{{ path('conference', { id: conference.id }) }}">View</a>
        </span>
        </h4>
        
    {% endfor %}


{% endblock %}

